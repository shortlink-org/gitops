CHART NAME: {{ .Chart.Name }}
CHART VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

** Please be patient while the chart is being deployed **

{{- $hf := .Values.cilium.hostFirewall | default dict }}
{{- if not $hf.enabled }}
NOTE: Cilium host firewall is disabled (cilium.hostFirewall.enabled=false).
      The host-lockdown CiliumClusterwideNetworkPolicy is not rendered — it only applies with host firewall.
{{- end }}
