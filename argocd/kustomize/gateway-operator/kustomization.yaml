apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Align with gateway-api:crd (standard channel); experimental-install conflicts with safe-upgrades VAP when standard CRDs exist.
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.5.1/standard-install.yaml
