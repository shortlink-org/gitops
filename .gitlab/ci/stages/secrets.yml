argo-secret:
  stage: secrets
  image:
    name: alpine/k8s:1.35.3
    entrypoint: [ "" ]
  before_script:
    - kubectl delete secret
      --namespace=argocd
      sops-gpg || true
  script:
    - kubectl create secret generic
      --namespace=argocd
      --from-file=sops.asc=${GPG_TOKEN}
      sops-gpg

    - kubectl create secret generic
      --namespace=argocd
      --from-file=sshPrivateKey=${ARGO_SSH_TOKEN}
      git-creds
  needs: []
  rules:
    - when: manual
